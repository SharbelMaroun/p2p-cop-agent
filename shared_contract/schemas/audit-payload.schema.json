{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "x-contract-version": "0.2.15-proposed",
  "x-role-neutral": true,
  "x-compatibility-profile": "simulator-v3.0.0",
  "title": "simulator-v3.0.0 AuditPayload",
  "description": "Post-game reveal delivered to submit_audit under the simulator-v3.0.0 compatibility profile. `series_consensus` is a peer extension (uoh-ay26, C-040): a post-series SHA-exchange envelope, never a game outcome -- valid only with empty records and a 64-hex consensus_sha, and it must never alter an already-completed sub-game result.",
  "type": "object",
  "required": [
    "sender",
    "records",
    "result_claim"
  ],
  "additionalProperties": true,
  "properties": {
    "sender": {
      "enum": [
        "police",
        "thief"
      ]
    },
    "records": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "payload",
          "nonce",
          "commit"
        ],
        "additionalProperties": true,
        "properties": {
          "payload": {
            "type": "object"
          },
          "nonce": {
            "type": "string",
            "pattern": "^[0-9a-f]+$",
            "description": "Per-turn commitment nonce, secret before this post-game audit reveal.",
            "x-purpose": "per-turn-commitment-nonce",
            "x-length-policy": "Lowercase hex; no length is imposed. The book fixes no nonce format (`inst/police_thief_p2p_Summary.md` shows `secrets.token_hex(16)` in its own example code but states no requirement), and Appendix E rule 19 sanctions a mismatch between the recomputed and declared digest (`:1270`), never an unusual shape. A peer whose digest verifies is honest whatever its nonce length; refusing it scored a fair player as a forger (`C-033`).",
            "x-visibility": "audit-only"
          },
          "commit": {
            "type": "string",
            "pattern": "^[0-9a-f]{64}$"
          }
        }
      }
    },
    "result_claim": {
      "description": "The sender's asserted outcome. Either one of our own enum strings, or a STRUCTURED claim carrying a non-empty `type` — group yanell11 send {type, violator, how}. Tolerated on receive since 0.2.15-proposed (C-048), never emitted: refusing the object form meant rejecting the very message that explained why a peer was refusing us, so our report and theirs then disagreed and rule 35 scored the conflict 0/0 on top of the original dispute. A claim of any shape is an assertion judged on evidence, never believed.",
      "oneOf": [
        {
          "enum": [
            "capture",
            "survival",
            "timeout",
            "series_consensus"
          ]
        },
        {
          "type": "object",
          "required": ["type"],
          "additionalProperties": true,
          "properties": {
            "type": {
              "type": "string",
              "minLength": 1
            }
          }
        }
      ]
    },
    "consensus_sha": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "series_consensus only: SHA-256 over the canonical game_id, game_uid and six agreed result/role/score/winner rows."
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "result_claim": {
            "const": "series_consensus"
          }
        }
      },
      "then": {
        "properties": {
          "records": {
            "maxItems": 0
          }
        },
        "required": [
          "consensus_sha"
        ]
      }
    }
  ]
}
