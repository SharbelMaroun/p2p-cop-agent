# REQUIREMENTS AUDIT: this entire file is an unverified draft. Provider and
# tunnel variables are not approved requirements. Do not provision credentials yet.

# LLM providers — OPTIONAL: the default 'template' trash-talk provider uses zero tokens
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Tunnel for league play (ngrok / Localtonet)
NGROK_AUTHTOKEN=your-ngrok-authtoken-here
