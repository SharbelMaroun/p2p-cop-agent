# Pre-Audit Documentation Archive

These documents were produced before direct requirements verification.

They contain unsupported numerical values, schemas, tool names, architecture, task counts, game counts, cryptographic fields, reporting behavior, and role assumptions. They are preserved only for history and idea recovery and must not guide implementation.

Only active documents under `docs/` and `CONFIRMED` entries in `docs/REQUIREMENTS_LEDGER.md` may guide work.
