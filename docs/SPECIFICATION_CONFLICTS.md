# Specification Conflicts and Resolutions

This register is not byte-identical with the Thief repository at the compared
baseline; see [PARITY_REPORT.md](PARITY_REPORT.md).

| ID | Status | Conflict | Resolution / required action |
|---|---|---|---|
| C-001 | CONFIRMED | Example `num_games: 1` versus six sub-games | One is an example sub-game/default; Appendix F table 18 fixes a played series at six. Do not promote the demo default. |
| C-002 | CONFIRMED | “Five components plus link” versus “six README sections” | They are the same mapping: five content sections plus section 6 companion link (book PDF p. 97; rule 42). |
| C-003 | RESOLVED / OPTION B | Exact MCP tool names | The book leaves names open. The 2026-07-28 project decision selects the Option-B profile: `negotiate`, `receive_turn`, `submit_audit` (exposed), optional `receive_control`; `exchange_audit` is a client transport method only and `receive_move` is excluded. ADR-001 accepted for this project; see [OPTION_B_DECISION.md](OPTION_B_DECISION.md). |
| C-004 | CONFIRMED | Lecturer/report address spellings differed | Canonical values are `rmisegal@gmail.com` and `rmisegal+uoh26finalgame@gmail.com`, on lecturer answer `AF-020`. **The book is not the authority here:** Appendix F table 20 prints `rimesegal@gmail.com`, `rimesegal+uoh26finalgame@gmail.com`, and `github.com/rimesegal/…`, while the Appendix B TOML sample prints `rmisegal+…`. Table 20's spelling is a source typo; the real upstream repository confirms `rmisegal`. A wrong reporting address silently loses game points (rule 32). |
| C-005 | CONFIRMED | Earlier text cited Appendix G for GitHub submission | Book v3.0.0 uses Appendix C. |
| C-006 | CONFIRMED | Existing docs claimed cross-repository byte parity | Baseline hashes differ and two claimed files are absent on Thief main. New contract remains unfrozen pending evidence. |
| C-007 | UNKNOWN | Duplicated stateless shared runtime package versus separate peers | A parity-controlled docs/config/fixture bundle is proposed; generic duplicated runtime code is not authorized by that proposal. |
| C-008 | CONFLICT | Appendix B shared config uses schema 1.2; local generated artifacts use 1.1; simulator runtime uses 1.3 | Record all three observations; do not normalize or claim compatibility without authoritative evidence. |
| C-009 | CONFIRMED | Simulator subtractive scent versus book multiplicative scent | Book Ch. 4 controls; use `max(0,(1-ρ)τ+Δτ)`. ADR-005 records the decision. |
| C-010 | UNKNOWN | Whether simulator source may be copied into this public MIT repository | Educational-use EULA/provenance review or lecturer permission is required. ADR-008 defaults to no substantial copying. |
| C-011 | UNKNOWN | Four local JSON files were called official templates but are byte-identical to generated simulator logs | Keep them as `NEEDS_MANUAL_REVIEW` observations until the original authenticated course handoff is supplied. |
| C-012 | RESOLVED | Appendix B names `game.json` and `rate_limits.json` but embeds Gatekeeper values in the signed game file | Each run explicitly loads the authoritative byte-identical match object and a local enforcement mirror. The mirror's Gatekeeper object must equal the signed match terms exactly; its own bytes and local extensions are not cross-repository match terms. Stable bundle examples are never runtime defaults. |
| C-013 | RESOLVED | Supplied agreed-config artifact reports schema `1.1`, while its recorded hash was produced from source shared terms with schema `1.3` | Source-config and emitted-artifact schema domains are distinct. This repository accepts source config `1.2`; it does not translate artifact 1.1 or simulator 1.3. |
| C-014 | RESOLVED | “Four JSON files” can be read as four physical files despite per-sub-game config/log names | It means four artifact families. A six-game series emits one declaration, six configs, six logs, and one aggregate result per reporting peer. |
| C-015 | CONFLICT / M7 | Upgrade plan says UUIDv4, current simulator derives a SHA-256-based UUID, and the supplied UUID has no RFC version | Do not freeze UUID derivation in M1. Select and test one accepted M7 protocol. |
| C-016 | CONFIRMED DISTINCTION | Eight-character Moodle team code was treated as runtime `group_id` syntax | Keep the administrative team code separate; runtime IDs remain non-empty text pending an artifact schema. |
| C-017 | CONFIRMED DISTINCTION | Book table 20 was described as twenty controlled repository paths | Table 20 lists artifact variables, repository, and addresses. The parity manifest is an internal Cop-authored coordination tool. |
| C-018 | CONFIRMED DISTINCTION | Simulator `validate_agreement`/stub success was treated as M1 contract acceptance | It checks nine required normalized terms. M1 review/parity and future runtime Step-0 are separate gates. |
| C-019 | RESOLVED / OPTION B | Per-turn commitment serialization and delimiter were undefined | The Option-B profile fixes `sha256(canonical_json(payload) + "\|" + nonce)` with a literal `\|` delimiter and a 16-byte/32-hex commitment nonce revealed only in the post-game audit. ADR-006 is accepted for this project. |
| C-020 | RESOLVED / OWNER CONFIRMATION | `negotiate.nonce` is public while per-turn commitment nonces are secret until audit | These are distinct purpose/lifecycle domains. The per-turn nonce is generated independently and must not reuse or derive from the public challenge; equal wire shape does not merge the domains. |
| C-021 | RESOLVED / BOOK ERROR | Book p. 43 states that at `ρ = 0.10` the factor `(1-ρ)` means "the existing scent is **reduced by 90%**" | Arithmetic error in the source. `(1 - 0.10) = 0.9`, so each turn **retains** 90% and reduces by 10%. Implement the formula `max(0, (1-ρ)·τ + Δτ)`, never the prose. Blocks `M6-01`/`M6-07`; disclose under the book p. 5 contradiction clause. |
| C-022 | RESOLVED / BOOK ERROR | Book p. 46 states that raising `ρ` toward 1.0 would leave the board "**saturated** with scent" | Reversed. `ρ → 1.0` drives `(1-ρ) → 0`, so scent vanishes almost immediately; saturation is what a `ρ → 0` would approach. Sensitivity work under `M9-06b` must use the correct direction. |
| C-023 | RESOLVED / M8 | Book ch. 7 (p. 74) verifies a replay step as `SHA256("{nonce}\|{move}")`, but ch. 5 (p. 53) seals `SHA256(canonical_json(state, move, intent, nonce))` | The two constructions cannot agree, so a replay viewer built literally from ch. 7 fails every commitment produced by ch. 5, and rule 19 makes any audit mismatch an automatic zero. The book itself footnotes p. 74 as simplified. Build `M8-02` from this repository's `protocol/` construction (see `C-019`), never from the ch. 7 sketch. **Not a conflict; the book resolves it in its own voice (verified 2026-08-06, `M8-02d`).** `:1757` (p. 58/146) footnotes the p. 74 listing directly: "the sketch simplified the input for the sake of the illustration; in practice the signature covers all components of the step — Intent, Move, State and Nonce — as detailed in the protocol in Chapter 5". Chapter 7 names chapter 5 as normative, so there is nothing to escalate and nothing is disclosed. The reference simulator diverges from p. 74 identically — its `verify_record` in `src/police_thief/gui/replay_data.py` recomputes over the revealed payload with the nonce, not over a bare move string. Reclassified from CONFLICT: leaving it here would have asked the lecturer to settle a question the source already settles. Action is unchanged — build the replay verifier from this repository's own construction, never from the ch. 7 sketch. |
| C-024 | RESOLVED | Appendix F table 15 sets `[Step Limit]` and `[Survival Threshold]` to the same value (35), which reads as leaving it undefined whether surviving *exactly* the threshold is a Thief win | Chapter 3 table 2 (PDF p. 38) settles it: the survival row is the Thief surviving "the limit of valid moves" without capture, and table 15 makes the limit and the threshold the same number. The horizon test is therefore **inclusive** — completing the final step uncaptured is a Thief win. `run_sub_game` already behaved this way; the reading is now recorded and pinned by a boundary test. `U-027` closed. |
| C-025 | CONFIRMED DISTINCTION | `[Number of Agents]` names two different parameters | Appendix F table 13 row 2 means players in a match (2, Fixed); table 18 row 1 means games in a series against one opponent (6, Fixed). Same bracket token, different meaning, both Fixed. Map them to `num_agents` and the series count respectively; never conflate. |
| C-026 | CONFIRMED | Appendix F table 17 lists five scoring rows and omits the technical-loss value, which the shared config schema requires as `technical_loss` | The value is fixed at 0/0 by table 2 (p. 38) and rule 48. The parameters table is incomplete, not permissive; do not treat the omission as freedom to choose. |
| C-027 | CONFIRMED DISTINCTION | The binding board size is 7×7, but illustrations use 10×10 (belief map), 6×6 (figure 1), 5×5 ("what if"), and 3×3 (GUI mock-up) | Only Appendix F table 13 binds. Build the board at the negotiated `grid_size` and size every matrix to the board; treat all figure dimensions as illustration. |
| C-028 | CONFIRMED DISTINCTION | The same two appendices are cited under different letters throughout the book | The mandatory-parameters table is called Appendix E, F, V, I, and "1" in different places, and its own heading on p. 151 reads "Appendix V"; the rules table is called Appendix D, E, and H. Resolve by content, not letter: the parameters table is the final appendix, the rules table is the 55-row Do/Don't mapping. |
| C-029 | RESOLVED / COORDINATOR | Six-sub-game role schedule was recorded here as unresolved (`U-025`) while the companion peer closed the same question | The coordinator relayed an authoritative lecturer answer on 2026-07-29: sub-games 1, 3, 5 use the natural role, 2, 4, 6 the swapped role, and the Thief moves first. `U-025` is closed on that authority. Provenance is a coordinator-relayed lecturer answer, not a Moodle post; it is recorded at that authority level. |
| C-030 | RESOLVED / SIMULATOR WIRE | The book's per-turn `Reveal` phase and the reference's actual wire disagree about whether the move is sent live | Chapter 5.3 and figure 6 define phase 3 as "the peers exchange their actual `Move` and `Hint`, while the `Nonce` remains hidden until the end of the match". The reference implementation sends **no move at all**: verified 2026-08-01, its live turn message carries only `step`, `sender`, `hint`, `smell_grid`, `commit`, `timestamp` and the optional claim members, while the move, the peer's true position, the bluff verdict, and the nonce all stay private until the post-game audit. This repository follows the reference, because the wire is what a classmate's agent must interoperate with and a peer that expects a live move would stall on every turn from a simulator-built opponent. The stricter behaviour is also safer: less is revealed, and nothing the book requires is lost — the move is still revealed, just at the audit rather than mid-turn. The phase machine keeps the mandated name `AWAITING_REVEAL` and documents it as "committed and owed the opponent's next turn" `[AE-4]` |

| C-031 | RESOLVED / OWNER — split decision | The book mandates the pre-game exchange carry members, repo URLs, MCP server URLs, hardware specs, the LLM model, and a `config_sha256` lock; our schema defined these identity members but marked none required, and `build_offer` accepted any identity dict (`M5-04h`, `U-029`) | Settled 2026-08-01 as **"populate ours, tolerate theirs."** The requirement splits cleanly: *populating our own outbound offer* with the full mandated content and the `config_sha256` lock is **contract-independent** (the `negotiate` schema already defines the members and is `additionalProperties: true`, so nothing in `shared_contract/` changes), whereas *requiring an opponent* to send them refuses a simulator-built peer that keeps these values in emitted artifacts rather than on the wire, and would lose interoperable matches. So this repository now always sends the full identity + lock (`protocol/identity.py`, `build_offer`) and **still tolerates** a peer that omits them (`verify_offer` unchanged). The enforcement half — making the fields `required` and refusing a peer — remains the coordinator's call and is tracked at `U-029`. This is an owner decision, not a silent default: the book *mandates* our content, and only the cross-peer refusal is deferred |
| C-033 | RESOLVED / RECORDED NUANCE | The book's turn cycle is **simultaneous** under commit-reveal (both sides commit, acknowledge, then reveal — book ch. 5, per the book notebook 2026-08-08), the lecturer's relayed ruling says "the Thief moves first" (`C-029`), and the local harness plays strict alternation (`THIEF_ACTION` then `COP_ACTION`) | All three are true at different layers, and none contradicts the others: "Thief moves first" governs **message order** — the Thief opens every turn cycle (`opens=True` on step 1) — while move *resolution* under commit/ack/reveal is simultaneous, and the harness's alternating schedule is a local **referee approximation** of that simultaneity, kept because it is the schedule the lecturer's ruling names and the conservative direction for our published numbers (an alternating cop reacts one step sooner than a committed one could). Recorded so nobody reads the harness order as the wire's move-resolution semantics; the wire itself carries no move at all (`C-030`) |
| C-032 | CONFIRMED | **Chapter 4.4's case study names its quadrants upside down.** The boxed "Thief Moves North" study (`inst/police_thief_p2p_Summary.md:1007-1022`) places the scent at `(1,4)`/`(1,3)` and calls that the **south-east** corner, then calls `(5,2)` a **northern** cell. Under the Appendix F table 13 origin — top-left, row growing downward, `CONFIRMED` in `PARAMETERS_BASELINE.md` — `(1,4)` is *north*-east and `(5,2)` is *southern*. North and south are swapped throughout the example. | Found 2026-08-06 while implementing `M6-02f`; the study is the **specification** for the lie-detection rule, so this was directly load-bearing. The **intensities** are authoritative and are used verbatim (fresh `0.81 = 0.9·(1−0.1)`, adjacent `0.63`, claimed direction `0.00`); the **cell labels** are not, and the scenario is reconstructed under the confirmed top-left origin instead. Consistent only if that figure uses a bottom-left origin or `(col,row)` ordering, neither of which is stated. Resolved under the book's p. 5 contradiction clause; no ruling needed, because Appendix F outranks a chapter illustration and the arithmetic is unaffected. A literal transcription would have pinned an inverted board into `test_trust.py` and `test_consume.py` — the belief would have chased every lie. |
| C-033 | CONFIRMED | **A verifying audit was being scored as forgery over its nonce's shape.** The controlled schemas pinned every nonce to `^[0-9a-f]{32}$`, and `verify_commit` ran the same check generation does, so a peer revealing `secrets.token_hex(32)` (64 hex) failed verification **even when its digest reproduced exactly** and was reported `TAMPERED`. | Found 2026-08-06 by probing our own peers with a classmate-shaped audit. The book does not support it: Appendix E rule 19 is "Reject any game technical mismatch during the audit phase" and the book **defines** that mismatch as "Any mismatch between the recomputed hash and the hash declared during the commitment phase" (`inst/police_thief_p2p_Summary.md:1270`). A different-length nonce that still reproduces the digest is not a mismatch — it is proof the peer never changed its move. The book shows `secrets.token_hex(16)` in its own example code (`:1189`, `:1211`) but states no requirement, and the reference validates no nonce format at all, only re-hashing what it is given. **Resolution:** generation stays strict (our nonce is always 32 lowercase hex); verification hashes whatever it receives. Schema patterns relaxed to `^[0-9a-f]+$` — still hex, no length opinion — across the audit record, audit payload, and negotiation challenge. Contract bundle bumped `0.2.5-proposed` → `0.2.6-proposed` under `G-18`. Rule 19 is an iron rule with no appeal, so a false accusation costs a fairly played game and slanders a classmate. |
| C-034 | RESOLVED | **The reference plays all six sub-games, both roles, in one process; we run one process per sub-game** | **Not a book conflict -- the book permits our shape explicitly.** Asked directly (book PDF, 2026-08-09): rule 1 is about the two *entities*, not about time -- "run the police code and the thief code in two completely separate processes" (Appendix E, p.126/268), whose purpose is Zero-Trust so neither agent can reach the other's memory or local truth (p.15/55). **Rule 53 settles it**: "it is permitted to change, update and improve the code between game and game -- but for every game you must record the exact commit id that was played" (p.131/275), and updating code requires stopping and restarting, so continuity cannot be required. The book's own operational example swaps roles by restarting: `python -m police_thief peer --role thief`, and for sub-games 2/4/6 "the teams must start the agent again with the updated role" (p.123/260, p.124/263). Artifacts agree -- each sub-game has its own `config_<id>_g<NN>.json` and `log_<id>_g<NN>.json`, while the pre-game declaration is the one file "static for the whole series". The **reference** differs by choice: no `--games` flag, series length from `network_and_league.num_games`, and a single `peer` invocation plays all six via `_play_all`/`role_for` without exiting. Both are legal; we follow the book. **Interop consequence, seen live against `amireman` on 2026-08-09:** a reference-shaped runner continues to sub-game 2 and meets a 502 during our process handoff. Any book-shaped opponent produces that gap -- including one that restarts to change code under rule 53 -- so a peer must retry rather than abort, which is the same defect we fixed in our own readiness probe that day (`M9-26`). Rule 6 mandates a watchdog so a peer never freezes waiting, and Appendix F table 19 sets a 30s response timeout and 60s watchdog `[AE-1]` `[AE-53]` |

Rows marked `OPTION B` are closed by the documented 2026-07-28 academic-freedom
project decision precisely because authoritative sources conflict or are silent;
this is not the same as silently adopting a simulator/example default. All other
unresolved rows still require their own authority and are not closed by a default.
