# PRD — Gatekeeper and Reporting

Status: professional gatekeeper requirement confirmed; reporting contract blocked.

`PS-008` confirms that external API calls must pass through a centralized gatekeeper.

Exact rate limits, queue depths, Gmail address, OAuth behavior, draft/send mode, attachments, artifact schemas, and reporting sequence remain `UNKNOWN`.

No provider-specific or numerical settings are approved.
